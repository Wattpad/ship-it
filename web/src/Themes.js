// Put Matierial UI themes used in multiple files here

import createMuiTheme from '@material-ui/core/styles/createMuiTheme'

export const standard = createMuiTheme({
    palette: {
        primary: {
            main: '#FF6612'
        },
        secondary: {
            main: '#FEAF0A'
        }
    }
})

export const link = createMuiTheme({
    palette: {
        primary: {
            main: '#000000'
        },
        secondary: {
            main: '#FEAF0A'
        }
    }
})